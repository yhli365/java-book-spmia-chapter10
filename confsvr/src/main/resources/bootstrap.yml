spring:
  application:
    name: configserver
  profiles:
    active:
      native
      #gitrepo

eureka:
  instance:
    preferIpAddress: true
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
        defaultZone: http://localhost:8761/eureka/
